We have the following datasets of interest:

1. `herald_pairs.jsonl` has pairs of nl and lean proofs
2. `herald_pairs_with_text.jsonl` includes 1 + the corresponding theorem statements stored in *nl_text* and *lean_text*
